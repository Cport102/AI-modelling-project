{
  "name": "dtgpt-server",
  "version": "1.0.0",
  "description": "Proxy server for DTGPT — keeps the Gemini API key server-side.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node tests/run-tests-v2.js"
  },
  "dependencies": {
    "@vercel/blob": "^2.3.1",
    "dotenv": "^16.4.5"
  }
}
